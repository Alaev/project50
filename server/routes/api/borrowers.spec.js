const server = require('../../index');
const mongoose = require('mongoose');
const chai = require('chai');
const chaiHttp = require('chai-http');
chai.use(chaiHttp);
chai.should();

// add borrower tests here see books spec for refs.
